/*** ** valid hdl but bogus chip ** ***/
CHIP KindaOk {
  IN a, b;
  /* this is a comment */
  OUT out;
  /*** ***
  Now we have a multiline comment.
  This will be ok too!
  *** */

  PARTS:
  Xor(a=a[0], b=b[0], out=c0);

  // another valid comment is here
}
